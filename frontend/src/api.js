// ==========================================
// API base URL
// ==========================================
// Keeping this in one place means when we deploy the backend on Render,
// we only need to change this ONE line instead of editing every file.
//
// While working locally, this points to our local backend server.
// After deploying, replace it with your Render backend URL, e.g:
// export const API_URL = "https://my-ecommerce-backend.onrender.com";

export const API_URL = "http://localhost:5000";
